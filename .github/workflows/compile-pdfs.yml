name: Compile Typst CVs and Letter

on:
  push:
    paths:
      - '**.typ'
      - 'metadata.toml'
      - 'modules_en/**'
      - 'src/**'
      - '.github/workflows/compile-pdfs.yml'
  pull_request:
    paths:
      - '**.typ'
      - 'metadata.toml'
      - 'modules_en/**'
      - 'src/**'
      - '.github/workflows/compile-pdfs.yml'
  workflow_dispatch:

concurrency:
  group: typst-compile-${{ github.ref }}
  cancel-in-progress: true

jobs:
  compile-pdfs:
    runs-on: ubuntu-24.04

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Typst
        uses: typst-community/setup-typst@v3
        with:
          typst-version: 'latest'

      - name: Verify Typst installation
        run: |
          set -euo pipefail
          echo "Checking Typst installation..."
          which typst
          typst --version
          echo "PATH: $PATH"

      - name: Install required fonts
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends fonts-roboto fonts-font-awesome unzip
          # Install Source Sans Pro
          mkdir -p ~/.local/share/fonts
          curl -fsSL -o /tmp/TTF-source-sans-3.052R.zip \
            https://github.com/adobe-fonts/source-sans/releases/download/3.052R/TTF-source-sans-3.052R.zip
          unzip -q /tmp/TTF-source-sans-3.052R.zip -d ~/.local/share/fonts/
          rm -f /tmp/TTF-source-sans-3.052R.zip
          fc-cache -f

      - name: Compile CVs and cover letter
        run: |
          set -euo pipefail
          echo "=== Compiling CVs and cover letter ==="

          # Verify Typst is available
          echo "Verifying Typst before compilation..."
          which typst
          typst --version
          echo ""

          # Compile CVs and cover letter
          for source in cv.typ cv_senior_pm.typ cv_superintendent.typ cv_estimator.typ cv_exec_summary.typ letter.typ; do
            output="${source%.typ}.pdf"
            echo "Compiling ${source}..."
            typst compile "${source}" "${output}"
          done

          echo "=== Compilation complete ==="
          ls -lh *.pdf

      - name: Upload PDFs as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cv-pdfs
          path: |
            cv.pdf
            cv_senior_pm.pdf
            cv_superintendent.pdf
            cv_estimator.pdf
            cv_exec_summary.pdf
            letter.pdf
          if-no-files-found: error

      - name: Commit and push PDFs
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Add generated PDFs
          git add *.pdf

          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: auto-compile PDFs from Typst sources [skip ci]"
            git push
          fi
